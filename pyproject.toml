[project]
name = "rheinwerk_mes"
version = "0.0.1"
description = "Rheinwerk Chemie GmbH — consolidated Core MES (Frappe app). Placeholder scaffold for the rationalised target repository."
requires-python = ">=3.10"
readme = "README.md"

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

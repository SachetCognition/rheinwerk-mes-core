"""Per-gate parity/divergence record generator (W1-10, URS-W1-031).

The verdicts are derived from executed characterisation contracts and the suite's
test-case citations; see `model.py`. Entry point: `python -m tools.behaviour.generate`.
"""

from __future__ import annotations
